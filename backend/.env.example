DATABASE_URL=postgresql+psycopg://crm:crm@localhost:5432/crm
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_MINUTES=60
REFRESH_TOKEN_DAYS=7
# Bootstrap admin only (seed). Create all other employees in Admin → Employees (local DB).
ADMIN_EMAIL=admin@crm.local
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Administrator
STORAGE_DIR=../storage
MAX_UPLOAD_MB=15
OPEN_LEAD_LIMIT=3
CORS_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173
# Email via Brevo — paste real values in your local .env (never commit the key)
EMAIL_ENABLED=true
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=E-Star CRM
OVERDUE_DIGEST_HOUR=9
